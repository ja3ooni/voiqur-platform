---
phase: 08-unit-tests
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - kiro/voiquyr/tests/test_auth.py
  - kiro/voiquyr/tests/test_gdpr_validator.py
  - kiro/voiquyr/tests/test_ai_act_validator.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "test_auth.py covers auth.py module with happy path, bad credentials, and expired token cases"
    - "test_gdpr_validator.py covers GDPR validator logic"
    - "test_ai_act_validator.py covers AI Act validator logic"
  artifacts:
    - path: "kiro/voiquyr/tests/test_auth.py"
      provides: "Unit tests for src/api/auth.py"
      min_lines: 50
    - path: "kiro/voiquyr/tests/test_gdpr_validator.py"
      provides: "Unit tests for compliance module"
      min_lines: 30
    - path: "kiro/voiquyr/tests/test_ai_act_validator.py"
      provides: "Unit tests for AI Act validator"
      min_lines: 30
  key_links:
    - from: "test_auth.py"
      to: "src/api/auth.py"
      via: "pytest imports"
      pattern: "from src.api.auth import"
    - from: "test_gdpr_validator.py"
      to: "src/compliance/gdpr_validator.py"
      via: "pytest imports"
    - from: "test_ai_act_validator.py"
      to: "src/compliance/ai_act_validator.py"
      via: "pytest imports"
---

<objective>
Create unit tests for auth, GDPR validator, and AI Act validator modules.

Purpose: Meet TEST-01, TEST-02, TEST-03 requirements - each test module passes with assertions on real validator logic.
Output: Three test files that pytest can run successfully.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create test_auth.py for src/api/auth.py</name>
  <files>kiro/voiquyr/tests/test_auth.py</files>
  <read_first>
- kiro/voiquyr/src/api/auth.py
- kiro/voiquyr/tests/conftest.py
  </read_first>
  <behavior>
    - Test auth.py module imports correctly
    - Test happy path: valid credentials return user and token
    - Test bad credentials: invalid password raises 401
    - Test expired token: token with past exp raises 401
  </behavior>
  <action>
Create test_auth.py with pytest test functions:
1. Test module imports: verify auth.py imports without error
2. Test create_access_token: returns valid JWT string
3. Test verify_token: accepts valid token, returns user data
4. Test verify_token: rejects invalid token with HTTPException
5. Test verify_token: rejects expired token with HTTPException
6. Test password hashing: bcrypt.hashpw/verify works correctly

Use pytest fixtures from conftest.py for mock User objects.
Use unittest.mock for patching asyncpg pool if needed.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && pytest tests/test_auth.py -v --tb=short</automated>
  </verify>
  <done>pytest tests/test_auth.py passes with test cases for happy path, bad credentials, and expired token</done>
</task>

<task type="tdd">
  <name>Task 2: Create test_gdpr_validator.py</name>
  <files>kiro/voiquyr/tests/test_gdpr_validator.py</files>
  <read_first>
- kiro/voiquyr/src/compliance/gdpr_validator.py
- kiro/voiquyr/tests/conftest.py
  </read_first>
  <behavior>
    - Test GDPR validator module imports
    - Test validate_data_transfer: allows EU-to-EU transfers
    - Test validate_data_transfer: blocks transfers outside EU without adequacy decision
    - Test GDPR consent requirements
  </behavior>
  <action>
Create test_gdpr_validator.py:
1. Test module imports correctly
2. Test GDPR data transfer validation (EU resident)
3. Test GDPR consent validation (valid consent true/false)
4. Test GDPR data minimization (only required fields)
5. Test GDPR right to erasure (deletion request handling)

If compliance/gdpr_validator.py doesn't exist, create a stub that tests can validate against.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && pytest tests/test_gdpr_validator.py -v --tb=short</automated>
  </verify>
  <done>pytest tests/test_gdpr_validator.py passes with assertions on GDPR validator logic</done>
</task>

<task type="tdd">
  <name>Task 3: Create test_ai_act_validator.py</name>
  <files>kiro/voiquyr/tests/test_ai_act_validator.py</files>
  <read_first>
- kiro/voiquyr/src/compliance/ai_act_validator.py
- kiro/voiquyr/tests/conftest.py
  </read_first>
  <behavior>
    - Test AI Act validator module imports
    - Test high-risk AI system identification
    - Test transparency requirements
    - Test human oversight requirements
  </behavior>
  <action>
Create test_ai_act_validator.py:
1. Test module imports correctly
2. Test AI system risk classification (high/medium/low)
3. Test transparency disclosure requirements
4. Test human oversight mechanism validation
5. Test prohibited AI practice detection

If compliance/ai_act_validator.py doesn't exist, create a stub that tests can validate against.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && pytest tests/test_ai_act_validator.py -v --tb=short</automated>
  </verify>
  <done>pytest tests/test_ai_act_validator.py passes with assertions on AI Act validator logic</done>
</task>

</tasks>

<verification>
- pytest tests/test_auth.py -v passes
- pytest tests/test_gdpr_validator.py -v passes
- pytest tests/test_ai_act_validator.py -v passes
- pytest --co -q shows no collection errors for these test files
</verification>

<success_criteria>
- test_auth.py covers auth.py with happy path, bad credentials, expired token cases
- test_gdpr_validator.py covers GDPR validator logic with assertions
- test_ai_act_validator.py covers AI Act validator logic with assertions
- pytest --co reports no collection errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-unit-tests/08-01-SUMMARY.md`
</output>