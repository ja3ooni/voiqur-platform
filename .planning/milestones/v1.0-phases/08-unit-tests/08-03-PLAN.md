---
phase: 08-unit-tests
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - kiro/voiquyr/tests/test_messaging.py
  - kiro/voiquyr/tests/test_orchestration.py
autonomous: true
requirements:
  - TEST-07
  - TEST-08

must_haves:
  truths:
    - "test_messaging.py covers messaging.py for message send/receive handling"
    - "test_orchestration.py covers orchestration.py for pipeline coordination"
  artifacts:
    - path: "kiro/voiquyr/tests/test_messaging.py"
      provides: "Unit tests for src/core/messaging.py"
      min_lines: 40
    - path: "kiro/voiquyr/tests/test_orchestration.py"
      provides: "Unit tests for src/core/orchestration.py"
      min_lines: 40
  key_links:
    - from: "test_messaging.py"
      to: "src/core/messaging.py"
      via: "pytest imports"
    - from: "test_orchestration.py"
      to: "src/core/orchestration.py"
      via: "pytest imports"
---

<objective>
Create unit tests for messaging and orchestration modules.

Purpose: Meet TEST-07, TEST-08 requirements - each test module passes with assertions on real logic.
Output: Two test files that pytest can run successfully.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create test_messaging.py</name>
  <files>kiro/voiquyr/tests/test_messaging.py</files>
  <read_first>
- kiro/voiquyr/src/core/messaging.py
- kiro/voiquyr/tests/conftest.py
  </read_first>
  <behavior>
    - Test messaging module imports
    - Test message composition
    - Test message routing to channels
    - Test message delivery status
  </behavior>
  <action>
Create test_messaging.py:
1. Test module imports correctly
2. Test message composition: create message with required fields
3. Test message routing: route to correct channel (SMS/WhatsApp/Slack/Telegram)
4. Test message delivery: handle delivery status callbacks
5. Test message templates: render template with variables
6. Test message queue: add/remove from queue (if async)
7. Test message rate limiting (if implemented)

If core/messaging.py doesn't exist, create a stub.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && pytest tests/test_messaging.py -v --tb=short</automated>
  </verify>
  <done>pytest tests/test_messaging.py passes</done>
</task>

<task type="tdd">
  <name>Task 2: Create test_orchestration.py</name>
  <files>kiro/voiquyr/tests/test_orchestration.py</files>
  <read_first>
- kiro/voiquyr/src/core/orchestration.py
- kiro/voiquyr/tests/conftest.py
  </read_first>
  <behavior>
    - Test orchestration module imports
    - Test pipeline initialization
    - Test agent coordination
    - Test error handling in orchestration
  </behavior>
  <action>
Create test_orchestration.py:
1. Test module imports correctly
2. Test pipeline initialization: create pipeline with agents
3. Test agent coordination: run agents in sequence
4. Test agent error handling: handle agent failures gracefully
5. Test pipeline shutdown: cleanup resources on exit
6. Test configuration loading: parse pipeline config
7. Test state management: track pipeline state transitions

If core/orchestration.py doesn't exist, create a stub.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && pytest tests/test_orchestration.py -v --tb=short</automated>
  </verify>
  <done>pytest tests/test_orchestration.py passes</done>
</task>

</tasks>

<verification>
- pytest tests/test_messaging.py -v passes
- pytest tests/test_orchestration.py -v passes
- pytest --co -q shows no collection errors for these test files
- All 8 test modules from TEST-01 to TEST-08 are covered
</verification>

<success_criteria>
- test_messaging.py covers messaging.py for message handling
- test_orchestration.py covers orchestration.py for pipeline coordination
- All 8 test modules (TEST-01 through TEST-08) created and passing
- pytest --co reports no collection errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-unit-tests/08-03-SUMMARY.md`
</output>