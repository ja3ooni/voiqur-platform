---
phase: 10-production-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/manifests/secrets.yaml
  - k8s/manifests/postgres.yaml
  - k8s/manifests/redis.yaml
  - k8s/helm/euvoice-platform/values.yaml
autonomous: true
requirements:
  - PROD-01
  - PROD-04
  - PROD-06
user_setup: []
must_haves:
  truths:
    - "helm install --dry-run passes with no changeme placeholders"
    - "PVCs use gp3-encrypted storage class"
    - "All secrets use external-secrets references, not raw values"
  artifacts:
    - path: "k8s/manifests/secrets.yaml"
      provides: "External secrets references"
      min_lines: 30
    - path: "k8s/manifests/postgres.yaml"
      provides: "PostgreSQL PVC with gp3-encrypted"
      contains: "storageClassName: gp3-encrypted"
    - path: "k8s/manifests/redis.yaml"
      provides: "Redis PVC with gp3-encrypted"
      contains: "storageClassName: gp3-encrypted"
  key_links:
    - from: "k8s/manifests/secrets.yaml"
      to: "external-secrets operator"
      via: "secretStoreref"
      pattern: "secretStoreRef"
    - from: "k8s/helm/euvoice-platform/values.yaml"
      to: "postgres.yaml"
      via: "persistence"
      pattern: "storageClass.*gp3"
---

<objective>
Replace hardcoded secrets with external-secrets references and configure PVCs with gp3-encrypted storage class

Purpose: PRODUCTION-READY SECURITY — No plaintext secrets in manifests, all data encrypted at rest
Output: Updated k8s manifests with external-secrets and proper storage classes
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@k8s/DEPLOYMENT_GUIDE.md

# Current k8s manifests state
k8s/manifests/secrets.yaml - Contains hardcoded placeholder values (changeme)
k8s/manifests/postgres.yaml - Missing storageClassName
k8s/manifests/redis.yaml - Missing storageClassName
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update secrets.yaml with external-secrets references</name>
  <files>k8s/manifests/secrets.yaml</files>
  <read_first>k8s/manifests/secrets.yaml</read_first>
  <action>
Replace all hardcoded placeholder values (changeme) with external-secrets references:

1. For JWT_SECRET: Change from `changeme` to `{"apiVersion": "external-secrets.io/v1", "kind": "ExternalSecret", "spec": {"secretStoreRef": {"name": "vault-gdpr", "kind": "SecretStore"}, "target": {"name": "jwt-secret"}}}`
2. For DATABASE_URL: Use external-secret reference with JDBC URL format
3. For REDIS_URL: Use external-secret reference
4. For DEEPGRAM_API_KEY: Use external-secret reference
5. For MISTRAL_API_KEY: Use external-secret reference
6. For STRIPE_SECRET_KEY: Use external-secret reference
7. For TWILIO_*: Use external-secret references for account_sid, auth_token, phone_number

Wrap all in a K8s ExternalSecret manifest that the external-secrets operator can sync from Vault or AWS Secrets Manager.
  </action>
  <acceptance_criteria>
- secrets.yaml contains no literal "changeme" strings
- All 7+ secrets use secretStoreRef references
- File is valid YAML that passes `kubectl apply --dry-run=client`
  </acceptance_criteria>
  <verify>
<automated>grep -c "changeme" k8s/manifests/secrets.yaml || echo "0"</automated>
  </verify>
  <done>secrets.yaml uses external-secrets references, no plaintext values</done>
</task>

<task type="auto">
  <name>Task 2: Add gp3-encrypted storage class to postgres.yaml</name>
  <files>k8s/manifests/postgres.yaml</files>
  <read_first>k8s/manifests/postgres.yaml</read_first>
  <action>
Add storage configuration to postgres.yaml:

1. Add `storageClassName: gp3-encrypted` to the PVC spec
2. Add `accessModes: ["ReadWriteOnce"]`
3. Add `resources.requests.storage: "10Gi"` (or match existing)
4. Add `volumeMode: Filesystem`

Example fragment:
```yaml
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp3-encrypted
```
  </action>
  <acceptance_criteria>
- postgres.yaml contains `storageClassName: gp3-encrypted`
- postgres.yaml contains valid PVC spec with accessModes
  </acceptance_criteria>
  <verify>
<automated>grep "storageClassName: gp3-encrypted" k8s/manifests/postgres.yaml</automated>
  </verify>
  <done>PostgreSQL PVC uses gp3-encrypted storage</done>
</task>

<task type="auto">
  <name>Task 3: Add gp3-encrypted storage class to redis.yaml</name>
  <files>k8s/manifests/redis.yaml</files>
  <read_first>k8s/manifests/redis.yaml</read_first>
  <action>
Add storage configuration to redis.yaml:

1. Add `storageClassName: gp3-encrypted` to the PVC spec
2. Add `accessModes: ["ReadWriteOnce"]`
3. Add `resources.requests.storage: "2Gi"` (or match existing)
4. Add `volumeMode: Filesystem`

Use the same pattern as postgres.yaml Task 2.
  </action>
  <acceptance_criteria>
- redis.yaml contains `storageClassName: gp3-encrypted`
- redis.yaml contains valid PVC spec with accessModes
  </acceptance_criteria>
  <verify>
<automated>grep "storageClassName: gp3-encrypted" k8s/manifests/redis.yaml</automated>
  </verify>
  <done>Redis PVC uses gp3-encrypted storage</done>
</task>

<task type="auto">
  <name>Task 4: Test helm dry-run passes</name>
  <files>k8s/helm/euvoice-platform/values.yaml</files>
  <read_first>k8s/helm/euvoice-platform/values.yaml</read_first>
  <action>
Run helm dry-run to verify all manifests are valid:

1. Check if helm chart exists at k8s/helm/euvoice-platform/
2. If exists: Run `helm install --dry-run --generate-name ./k8s/helm/euvoice-platform`
3. If not exists: Create minimal values.yaml with the external-secrets references wired in
4. Verify no "changeme" values remain in the chart templates
  </action>
  <acceptance_criteria>
- helm install --dry-run succeeds with exit code 0
- No "changeme" strings in helm output
  </acceptance_criteria>
  <verify>
<automated>helm template ./k8s/helm/euvoice-platform 2>&1 | grep -c "changeme" || echo "0"</automated>
  </verify>
  <done>Helm chart passes dry-run validation</done>
</task>

</tasks>

<verification>
- [ ] secrets.yaml has no "changeme" values
- [ ] postgres.yaml uses gp3-encrypted
- [ ] redis.yaml uses gp3-encrypted
- [ ] helm --dry-run passes
</verification>

<success_criteria>
Valid manifests with external-secrets and encrypted PVCs deployed
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-readiness/10-01-SUMMARY.md`
</output>