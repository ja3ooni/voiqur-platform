---
phase: 07-frontend-command-center
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - kiro/voiquyr/frontend/src/services/audioStreamService.ts
  - kiro/voiquyr/frontend/.env.example
  - kiro/voiquyr/frontend/src/config/environment.ts
autonomous: true
requirements:
  - FE-02
user_setup: []

must_haves:
  truths:
    - "audioStreamService reads WebSocket URL from environment variable"
    - ".env.example documents REACT_APP_API_URL"
    - "No hardcoded ws:// URLs in audioStreamService.ts"
  artifacts:
    - path: kiro/voiquyr/frontend/src/services/audioStreamService.ts
      provides: WebSocket audio streaming
      contains: "REACT_APP_API_URL"
    - path: kiro/voiquyr/frontend/.env.example
      provides: Environment variable documentation
      min_lines: 1
  key_links:
    - from: audioStreamService.ts
      to: environment config
      via: process.env.REACT_APP_API_URL
      pattern: "process\\.env\\.REACT_APP_API_URL"
---

<objective>
Fix WebSocket URL to use environment variable instead of hardcoded string. The service currently has `ws://localhost:8000/ws/audio` hardcoded on line 25.
</objective>

<read_first>
- kiro/voiquyr/frontend/src/services/audioStreamService.ts
- kiro/voiquyr/frontend/.env.example
</read_first>

<tasks>

<task type="auto">
  <name>Task: Fix WebSocket URL to use environment variable</name>
  <files>kiro/voiquyr/frontend/src/services/audioStreamService.ts</files>
  <action>
Change line 25 from:
```typescript
private wsUrl: string = 'ws://localhost:8000/ws/audio',
```
To:
```typescript
private wsUrl: string = process.env.REACT_APP_API_URL || 'ws://localhost:8000/ws/audio',
```
  </action>
  <verify>
grep -n "REACT_APP_API_URL" kiro/voiquyr/frontend/src/services/audioStreamService.ts
  </verify>
  <done>audioStreamService reads WebSocket URL from REACT_APP_API_URL env var</done>
</task>

<task type="auto">
  <name>Task: Document REACT_APP_API_URL in .env.example</name>
  <files>kiro/voiquyr/frontend/.env.example</files>
  <action>
Add to .env.example:
```
# WebSocket API URL for audio streaming (use wss:// for production)
REACT_APP_API_URL=wss://api.example.com/ws/audio
```
  </action>
  <verify>
grep -q "REACT_APP_API_URL" kiro/voiquyr/frontend/.env.example && echo "found" || echo "missing"
  </verify>
  <done>.env.example documents REACT_APP_API_URL</done>
</task>

</tasks>

<verification>
- [ ] audioStreamService.ts contains REACT_APP_API_URL
- [ ] .env.example documents REACT_APP_API_URL
- [ ] No hardcoded ws:// URLs remain
</verification>

<success_criteria>
audioStreamService.ts uses process.env.REACT_APP_API_URL for WebSocket URL (fallback to localhost for dev)
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-command-center/07-01-SUMMARY.md`
</output>