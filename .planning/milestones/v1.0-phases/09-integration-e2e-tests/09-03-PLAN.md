---
phase: 09-integration-e2e-tests
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - kiro/voiquyr/tests/test_compliance_e2e.py
autonomous: true
requirements:
  - TEST-11
must_haves:
  truths:
    - EU data residency is enforced (no data leaves EU-resident endpoint)
    - EU_DATA_RESIDENCY=True cannot be bypassed in production
    - GDPR mode is active
  artifacts:
    - path: "kiro/voiquyr/tests/test_compliance_e2e.py"
      provides: "Compliance E2E tests for EU data residency enforcement"
      min_lines: 80
  key_links:
    - from: "test_compliance_e2e.py"
      to: "src.api.config.APIConfig"
      via: "APIConfig(eu_data_residency=True)"
      pattern: "eu_data_residency"
    - from: "test_compliance_e2e.py"
      to: "src.compliance.gdpr_validator"
      via: "validate_data_transfer()"
      pattern: "GDPRValidator"
---

<objective>
Create compliance E2E test for EU data residency enforcement.

Purpose: TEST-11 requires confirming no data leaves the EU-resident endpoint (verified via APIConfig.eu_data_residency = True enforcement). This plan tests that the compliance layer correctly blocks non-EU data transfers.
Output: test_compliance_e2e.py with EU residency enforcement tests
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-integration-e2e-tests/09-01-SUMMARY.md
@kiro/voiquyr/src/api/config.py
@kiro/voiquyr/src/compliance/gdpr_validator.py

# EU compliance from Phase 1:
# - APIConfig.eu_data_residency: bool = True (default)
# - APIConfig.gdpr_mode: bool = True (default)

# These are the enforcement points to test
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify APIConfig defaults to EU residency</name>
  <files>kiro/voiquyr/tests/test_compliance_e2e.py</files>
  <action>
Create test_compliance_e2e.py with:

1. Test test_eu_data_residency_default_enabled():
   - Create APIConfig() with no arguments
   - Assert config.eu_data_residency == True
   - Assert config.gdpr_mode == True

2. Test test_eu_data_residency_cannot_be_disabled_in_production():
   - Check if PRODUCTION env var is set
   - If production mode, assert eu_data_residency cannot be set to False
   - This prevents accidental disable

Document with: `# TEST-11: EU data residency enforcement`
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -m pytest tests/test_compliance_e2e.py::test_eu_data_residency_default_enabled --collect-only 2>&1 | head -5</automated>
  </verify>
  <done>test_eu_data_residency_default_enabled passes</done>
</task>

<task type="auto">
  <name>Task 2: Test GDPR validator blocks external transfers</name>
  <files>kiro/voiquyr/tests/test_compliance_e2e.py</files>
  <action>
Add tests for GDPRValidator:

1. Test test_gdpr_validator_rejects_external_transfer():
   - Create GDPRValidator instance
   - Try to validate a data transfer to non-EU endpoint
   - Assert the transfer is rejected or flagged

2. Test test_gdpr_validator_allows_internal_transfer():
   - Validate transfer to internal EU endpoint
   - Assert transfer is allowed

3. Test test_gdpr_mode_blocks_data_export():
   - With gdpr_mode=True, assert data export API rejects non-EU targets
   - This tests the runtime enforcement layer

These tests verify the GDPR validator enforcement layer works.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -m pytest tests/test_compliance_e2e.py::test_gdpr_validator_rejects_external_transfer --collect-only 2>&1 | head -5</automated>
  </verify>
  <done>test_gdpr_validator_* tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Test end-to-end compliance flow</name>
  <files>kiro/voiquyr/tests/test_compliance_e2e.py</files>
  <action>
Add test_e2e_compliance_flow():

1. Use real API client (AsyncClient from conftest.py)
2. Set APIConfig with eu_data_residency=True
3. Make API call that would transfer data to external service
4. Assert the response includes proper EU compliance headers
5. Assert no non-EU endpoints are called

Test scenarios:
- /health endpoint returns EU compliance status
- External API call with eu_residency enabled blocks non-EU targets
- Data export endpoints enforce GDPR mode
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -m pytest tests/test_compliance_e2e.py::test_e2e_compliance_flow --collect-only 2>&1 | head -5</automated>
  </verify>
  <done>test_e2e_compliance_flow passes with EU enforcement</done>
</task>

<task type="auto">
  <name>Task 4: Test audit logging for compliance</name>
  <files>kiro/voiquyr/tests/test_compliance_e2e.py</files>
  <action>
Add test_audit_compliance_logging():

1. With audit system from earlier phase, write a compliance-relevant action
2. Read back audit logs
3. Assert audit log contains correct eu_data_residency=True marker
4. Assert audit log shows GDPR compliance was checked

This tests the audit trail for compliance - required for GDPR Article 30.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -m pytest tests/test_compliance_e2e.py::test_audit_compliance_logging --collect-only 2>&1 | head -5</automated>
  </verify>
  <done>test_audit_compliance_logging passes</done>
</task>

</tasks>

<verification>
- test_compliance_e2e.py has tests for APIConfig defaults, GDPR validator, E2E flow, audit logging
- All tests link to TEST-11 requirement
- Tests verify EU enforcement cannot be bypassed
</verification>

<success_criteria>
- TEST-11: Compliance E2E test confirms no data leaves the EU-resident endpoint
- Enforced via APIConfig.eu_data_residency = True enforcement
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-e2e-tests/09-03-SUMMARY.md`
</output>