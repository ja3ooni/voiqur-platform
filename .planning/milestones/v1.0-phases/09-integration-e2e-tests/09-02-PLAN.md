---
phase: 09-integration-e2e-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - kiro/voiquyr/tests/test_e2e_call_flow.py
autonomous: true
requirements:
  - TEST-10
must_haves:
  truths:
    - E2E call flow test executes audio → STT → LLM → TTS without mocks
    - Test produces non-empty audio bytes output
    - Each pipeline stage is verified independently
  artifacts:
    - path: "kiro/voiquyr/tests/test_e2e_call_flow.py"
      provides: "End-to-end call flow test covering full voice pipeline"
      min_lines: 100
  key_links:
    - from: "test_e2e_call_flow.py"
      to: "src.agents.stt_agent.VoxtralModelManager"
      via: "await mgr.transcribe(chunk)"
      pattern: "VoxtralModelManager"
    - from: "test_e2e_call_flow.py"
      to: "src.agents.llm_agent.LLMAgent"
      via: "await agent.process_message()"
      pattern: "LLMAgent"
    - from: "test_e2e_call_flow.py"
      to: "src.agents.tts_agent.XTTSv2ModelManager"
      via: "await mgr.synthesize()"
      pattern: "XTTSv2ModelManager"
---

<objective>
Create the E2E call flow test: audio file in → STT → LLM → TTS → response audio bytes out.

Purpose: TEST-10 requires the full call flow test to complete without mocks and produce non-empty audio bytes. This plan wires together STT, LLM, and TTS stages from Phases 2-4 into a complete E2E test.
Output: test_e2e_call_flow.py with complete pipeline test
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-integration-e2e-tests/09-01-SUMMARY.md
@kiro/voiquyr/tests/test_stt_agent.py
@kiro/voiquyr/tests/test_llm_agent.py
@kiro/voiquyr/tests/test_tts_agent.py
@kiro/voiquyr/src/core/processing_pipeline.py

# Pipeline components from prior phases:
# - VoxtralModelManager (STT - Phase 2)
# - LLMAgent/LLMManger (LLM - Phase 3)
# - XTTSv2ModelManager (TTS - Phase 4)

# Note: This plan depends on Phases 2, 3, 4 being complete (STT/LLM/TTS real implementations)
# If those phases are not complete, tests will fail - that's expected and tracks progress
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test audio file fixture</name>
  <files>kiro/voiquyr/tests/test_e2e_call_flow.py</files>
  <action>
Create test_e2e_call_flow.py with:

1. Imports: VoxtralModelManager, LLMAgent/XTTSv2ModelManager, AudioChunk
2. `@pytest.fixture` `sample_audio()`:
   - Generates sine wave tone (440Hz, 1 second, 16kHz sample rate)
   - Returns AudioChunk with np.float32 data
   - This avoids external file dependencies
3. `@pytest.fixture` `pipeline_config`:
   - Returns dict with model configurations
   - Marks tests to skip if required API keys are not set

The fixtures should use `@pytest.fixture(autouse=True)` for convenience or explicit per-test usage.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -m pytest tests/test_e2e_call_flow.py --collect-only 2>&1 | head -10</automated>
  </verify>
  <done>test_e2e_call_flow.py has sample_audio and pipeline_config fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Test STT stage</name>
  <files>kiro/voiquyr/tests/test_e2e_call_flow.py</files>
  <action>
Add test_stage_stt():

1. Create VoxtralModelManager and load model
2. Get sample audio via fixture
3. Call await mgr.transcribe(chunk)
4. Assert:
   - result.text is a non-empty string
   - result.text is NOT the mock string "Transcribed audio chunk {id}"
   - result.confidence is not None (if available)
5. Clean up: await mgr.unload_model()

Document with: `# TEST-10: STT stage of E2E call flow`
  </action>
  <verify>
    <automated> cd kiro/voiquyr && python -m pytest tests/test_e2e_call_flow.py::test_stage_stt --collect-only 2>&1 | head -5</automated>
  </verify>
  <done>test_stage_stt passes with real transcript</done>
</task>

<task type="auto">
  <name>Task 3: Test LLM stage</name>
  <files>kiro/voiquyr/tests/test_e2e_call_flow.py</files>
  <action>
Add test_stage_llm():

1. Create LLMAgent and initialize
2. Process a test message ("Hello, what time is it?")
3. Assert:
   - response is a non-empty string
   - response is NOT a mock response
   - 'tool_calls' or 'response' key is present in result
4. Clean up: await agent.shutdown()

Document with: `# TEST-10: LLM stage of E2E call flow`

Note: If LLM phase not complete, this may fail - that's intentional for tracking.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -m pytest tests/test_e2e_call_flow.py::test_stage_llm --collect-only 2>&1 | head -5</automated>
  </verify>
  <done>test_stage_llm passes with real Mistral response</done>
</task>

<task type="auto">
  <name>Task 4: Test TTS stage</name>
  <files>kiro/voiquyr/tests/test_e2e_call_flow.py</files>
  <action>
Add test_stage_tts():

1. Create XTTSv2ModelManager and load model
2. Synthesize text ("Hello world")
3. Assert:
   - result.audio is not None
   - len(result.audio) > 0 (non-empty audio bytes)
   - sample rate is correct (e.g., 24000 for ElevenLabs)
4. Clean up: await mgr.unload_model()

Document with: `# TEST-10: TTS stage of E2E call flow`

Note: If TTS phase not complete, this may fail - that's intentional for tracking.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -m pytest tests/test_e2e_call_flow.py::test_stage_tts --collect-only 2>&1 | head -5</automated>
  </verify>
  <done>test_stage_tts passes with non-zero audio bytes</done>
</task>

<task type="auto">
  <name>Task 5: Full E2E pipeline test</name>
  <files>kiro/voiquyr/tests/test_e2e_call_flow.py</files>
  <action>
Add test_full_call_flow() that wires all 3 stages together:

1. STT stage: transcribe sample audio → get transcript
2. LLM stage: send transcript to LLMAgent → get response text
3. TTS stage: synthesize response text → get audio bytes

Assert:
- Each stage produces valid output
- Final audio bytes are non-empty
- Audio format is correct (bytes, not None)

This is the primary TEST-10 acceptance test.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -m pytest tests/test_e2e_call_flow.py::test_full_call_flow --collect-only 2>&1 | head -5</automated>
  </verify>
  <done>test_full_call_flow coordinates all 3 pipeline stages</done>
</task>

</tasks>

<verification>
- test_e2e_call_flow.py has 5 test functions
- Each stage tests independently
- Full pipeline test wires all stages together
- All tests skip gracefully if required API keys unavailable
</verification>

<success_criteria>
- TEST-10: E2E call flow test completes without mocks and produces non-empty audio bytes
- Test documents which API key enables which stage
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-e2e-tests/09-02-SUMMARY.md`
</output>