---
phase: 09-integration-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kiro/voiquyr/tests/conftest.py
autonomous: true
requirements:
  - TEST-09
must_haves:
  truths:
    - Integration tests connect to test PostgreSQL using asyncpg pool
    - Integration tests connect to test Redis
    - DB fixtures are reusable across test modules
  artifacts:
    - path: "kiro/voiquyr/tests/conftest.py"
      provides: "DB pool and Redis fixtures with asyncpg and redis.asyncio"
      min_lines: 70
    - path: "kiro/voiquyr/tests/fixtures.py"
      provides: "Reusable test data fixtures (users, sessions, knowledge items)"
      min_lines: 50
  key_links:
    - from: "kiro/voiquyr/tests/conftest.py"
      to: "kiro/voiquyr/src/api/main.py"
      via: "AsyncClient with ASGITransport"
      pattern: "AsyncClient.*ASGITransport"
---

<objective>
Set up real DB fixtures for integration tests, ensuring integration tests spin up against a real test PostgreSQL database and pass DB read/write assertions.

Purpose: TEST-09 requires integration tests to use a real pytest-asyncio + asyncpg pool - this plan expands the existing conftest.py to provide reusable fixtures for Phase 9 integration tests and beyond.
Output: Enhanced conftest.py with reusable DB fixtures
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@kiro/voiquyr/tests/conftest.py
@kiro/voiquyr/pytest.ini

# From Phase 1: conftest.py already provides db_pool and redis_client fixtures
# From Phase 8 (in planning): unit test structure

Existing fixtures structure (conftest.py):
- TEST_DATABASE_URL from env or DATABASE_URL
- TEST_REDIS_URL from env or REDIS_URL
- db_pool fixture: asyncpg pool, skips if DB unavailable
- redis_client fixture: redis.asyncio client, skips if Redis unavailable
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance conftest.py with reusable DB fixtures</name>
  <files>kiro/voiquyr/tests/conftest.py</files>
  <action>
Expand conftest.py with additional test fixtures:

1. Add `db_pool` already exists - verify it's properly configured for integration tests
2. Add `test_user` fixture: creates a test user in the DB, yields, and cleans up
3. Add `test_session` fixture: creates test session, links to user
4. Add `test_knowledge_item` fixture: creates test knowledge item
5. Add `db_cleaner` fixture: ensures clean state between tests

The fixtures should:
- Accept optional parameters for customization
- Handle cleanup automatically via yield
- Skip gracefully if DB is unavailable (pytest.skip)
- Support asyncpg transactions for test isolation (use explicit delete in teardown)
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -c "import pytest; print('conftest imports OK')"</automated>
  </verify>
  <done>conftest.py has db_pool with session scope, test_user, test_session, test_knowledge_item, and db_cleaner fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Create fixtures.py module for data factories</name>
  <files>kiro/voiquyr/tests/fixtures.py</files>
  <action>
Create a new `tests/fixtures.py` module with factory functions for reusable test data:

1. `create_user(db_pool, **overrides)` - returns dict with user data (id, email, password_hash, created_at)
2. `create_session(db_pool, user_id, **overrides)` - returns dict with session data
3. `create_knowledge_item(db_pool, user_id, **overrides)` - returns dict with knowledge item
4. `create_audit_log(db_pool, user_id, action, **overrides)` - returns dict with audit log entry
5. `create_webhook_registration(db_pool, user_id, url, events, **overrides)` - returns dict

Each factory:
- Generates unique IDs using UUID4
- Defaults for required fields (timestamps, status values)
- Accepts overrides for customization
- Returns the created record dict (not model object)
- Can be used with raw asyncpg queries
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -c "from tests.fixtures import create_user; print('fixtures.py imports OK')"</automated>
  </verify>
  <done>tests/fixtures.py exists with factory functions for users, sessions, knowledge_items, audit_logs, webhooks</done>
</task>

<task type="auto">
  <name>Task 3: Create integration test example using real DB</name>
  <files>kiro/voiquyr/tests/test_integration_db.py</files>
  <action>
Create `tests/test_integration_db.py` demonstrating real DB integration testing:

1. Test user creation: insert test_user, query back, verify data
2. Test session creation: create user + session, verify foreign key
3. Test knowledge CRUD: create, read, update, delete knowledge item
4. Test audit logging: write audit log, read back by user_id

Each test:
- Uses the `db_pool` fixture from conftest.py
- Uses factory functions from fixtures.py
- Has assertions on real DB read/write behavior
- Cleans up test data in teardown
- Test docstrings link to requirements (TEST-09)
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -m pytest tests/test_integration_db.py --collect-only 2>&1 | head -20</automated>
  </verify>
  <done>test_integration_db.py has 4 tests covering user, session, knowledge, and audit CRUD operations</done>
</task>

</tasks>

<verification>
- conftest.py contains all required fixtures
- fixtures.py contains factory functions
- test_integration_db.py collects without errors
- Each test uses real db_pool fixture
</verification>

<success_criteria>
- TEST-09: Integration tests spin up a real asyncpg pool against test PostgreSQL and pass DB read/write assertions
- Fixtures are reusable across test modules (not hardcoded per test)
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-e2e-tests/09-01-SUMMARY.md`
</output>