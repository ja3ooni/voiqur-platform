---
phase: 03-llm
plan: 03
type: tdd
wave: 3
depends_on:
  - 03-02
files_modified:
  - kiro/voiquyr/tests/test_llm_agent.py
autonomous: true
requirements:
  - LLM-05

must_haves:
  truths:
    - "Multi-turn conversation (3+ turns) correctly passes full messages history to each Mistral API call"
    - "Tool call execution verified by tests produces correct results"
  artifacts:
    - path: kiro/voiquyr/tests/test_llm_agent.py
      provides: LLM agent tests
      contains: test_multi_turn_conversation, test_tool_call_execution
  key_links:
    - from: test_multi_turn_conversation
      to: MistralModelManager.generate_response
      via: 3+ generate_response calls with accumulated messages
      pattern: for.*in.*range\(3\).*generate_response
    - from: test_tool_call_execution
      to: ToolExecutor.execute_tool_call
      via: mock tool + verify result
      pattern: execute_tool_call.*weather
---

<objective>
Create multi-turn conversation test with tool call execution verification

Purpose: LLM-05 requires multi-turn conversation with tool call execution verified by tests.

Output: Pass/fail test results for multi-turn tool call flow
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-llm/03-CONTEXT.md

# Plan 02 output context
@kiro/voiquyr/src/agents/llm_agent.py (LLM Agent with tool integration)
@kiro/voiquyr/src/agents/tool_integration.py (ToolRegistry, ToolExecutor)
@kiro/voiquyr/tests/test_llm_agent.py (existing test file)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write multi-turn conversation test (TDD - RED phase)</name>
  <files>kiro/voiquyr/tests/test_llm_agent.py</files>
  <behavior>
    - Test 1: 3-turn conversation - each turn's response uses previous messages in history
    - Test 2: Messages accumulate correctly across turns (history length increases)
    - Test 3: Each turn receives context from all prior turns
    - Test 4: Tool call in turn 2 executes and result flows to turn 3
  </behavior>
  <action>
    Add TDD tests for multi-turn conversation:

    1. Add import for unittest.mock to patch MistralClient
    2. Add test_multi_turn_conversation():
       - Mock MistralClient.chat to return responses for each call
       - Create ConversationContext with session_id
       - Turn 1: Add user message "What's the weather?", get response
       - Turn 2: Add user message "In London please", get response
       - Turn 3: Add user message "Thanks!", get response
       - Assert each response was generated with accumulated messages
       - Assert history length = 6 messages (3 user + 3 assistant)

    3. Mark tests as async with pytest.mark.asyncio

    This is RED phase - tests will fail until we implement properly.
  </action>
  <verify>
    <automated>pytest tests/test_llm_agent.py::test_multi_turn_conversation -v 2>&1 | head -30</automated>
  </verify>
  <done>Multi-turn test written and fails (RED phase complete)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tool call execution test (TDD - RED phase)</name>
  <files>kiro/voiquyr/tests/test_llm_agent.py</files>
  <behavior>
    - Test 1: Tool call response triggers execution
    - Test 2: Tool result flows back to Mistral API
    - Test 3: Final response includes tool execution result
    - Test 4: Error in tool execution handled gracefully
  </behavior>
  <action>
    Add TDD tests for tool call execution:

    1. Add test_tool_call_execution():
       - Mock MistralClient to return tool_call response first time
       - Mock ToolExecutor to return weather result
       - Assert tool executed with correct arguments
       - Assert second call includes tool result

    2. Add test_tool_execution_error_handling():
       - Mock ToolExecutor to raise exception
       - Assert error is logged and user notified

    These are RED phase tests - will fail until Plan 02 is complete.
  </action>
  <verify>
    <automated>pytest tests/test_llm_agent.py::test_tool_call_execution -v 2>&1 | head -30</automated>
  </verify>
  <done>Tool call test written and fails (RED phase complete)</Done>
</task>

<task type="auto">
  <name>Task 3: Run tests and verify GREEN phase</name>
  <files>kiro/voiquyr/tests/test_llm_agent.py</files>
  <action>
    With Plans 03-01 and 03-02 complete, run the multi-turn and tool call tests:

    1. Set MISTRAL_API_KEY in test environment (mock or skip if not set)
    2. Run: pytest tests/test_llm_agent.py::test_multi_turn_conversation -v
    3. Run: pytest tests/test_llm_agent.py::test_tool_call_execution -v
    4. If tests fail: fix implementation, re-run

    Tests should pass now that:
    - Real Mistral API is wired (Plan 03-01)
    - Tool calling and context history work (Plan 03-02)
  </action>
  <verify>
    <automated>pytest tests/test_llm_agent.py -k "multi_turn or tool_call" -v --tb=short</automated>
  </verify>
  <done>All multi-turn and tool call tests pass (GREEN phase complete)</done>
</task>

</tasks>

<verification>
- test_multi_turn_conversation passes
- test_tool_call_execution passes  
- test_tool_execution_error_handling passes
- No mocking of internal functions (real API or clean mocks only)
</verification>

<success_criteria>
LLM-05: true - Multi-turn conversation with tool call execution verified by tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm/03-03-SUMMARY.md`
</output>