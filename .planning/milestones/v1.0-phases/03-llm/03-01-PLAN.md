---
phase: 03-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kiro/voiquyr/src/agents/llm_agent.py
  - kiro/voiquyr/tests/test_llm_agent.py
autonomous: true
requirements:
  - LLM-01
  - LLM-02

must_haves:
  truths:
    - "_load_mistral_small_31() uses real mistralai SDK (MistralClient.chat()) instead of local transformers"
    - "_generate_mock_response() no longer exists in the codebase"
    - "When MISTRAL_API_KEY is missing, system raises ConfigurationError rather than returning mock"
  artifacts:
    - path: kiro/voiquyr/src/agents/llm_agent.py
      provides: MistralModelManager class
      contains: MistralModelManager, generate_response, _load_mistral_small_31
    - path: kiro/voiquyr/tests/test_llm_agent.py
      provides: LLM agent tests
      contains: test_mistral_model_manager_init
  key_links:
    - from: MistralModelManager.generate_response
      to: mistralai.Mistral client (via src/api/models/__init__)
      via: _mistral_client.chat() call
      pattern: MistralClient\.chat\(
    - from: Config check
      to: Environment variable
      via: os.getenv("MISTRAL_API_KEY")
      pattern: MISTRAL_API_KEY
---

<objective>
Replace the mock response generator with real Mistral API inference

Purpose: LLM-01 requires real Mistral API via SDK, LLM-02 requires removing mock. This plan wires MistralModelManager to use the real Mistral cloud API from src/api/models/__init__.

Output: MistralModelManager using MistralClient.chat() for response generation
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-llm/03-CONTEXT.md

# Existing code per 03-CONTEXT.md
@kiro/voiquyr/src/api/models/__init__.py (lines 49-92, _mistral_client initialization)
@kiro/voiquyr/src/agents/llm_agent.py (lines 142-339, MistralModelManager class)

# Test files
@kiro/voiquyr/tests/test_llm_agent.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test stubs for real Mistral API integration</name>
  <files>kiro/voiquyr/tests/test_llm_agent.py</files>
  <behavior>
    - Test 1: MistralModelManager initializes with valid API key and returns model info
    - Test 2: generate_response() calls MistralClient.chat() when API key available
    - Test 3: generate_response() raises ConfigurationError when MISTRAL_API_KEY is missing
    - Test 4: Mock response generation should NOT be called (verify _generate_mock_response not invoked)
  </behavior>
  <action>
    Add test stubs to test_llm_agent.py using pytest.mark.xfail for now:
    1. Import Mistral from mistralai (handle ImportError gracefully)
    2. Import os for environment variable access
    3. Add test_mistral_api_client_init - tests _mistral_client initialization from src/api/models
    4. Add test_generate_uses_real_api - verifies MistralClient.chat() is called
    5. Add test_missing_api_key_raises_error - verifies ConfigurationError raised
    6. Add test_mock_response_removed - verifies _generate_mock_response does not exist

    Run: pytest tests/test_llm_agent.py -v to verify stubs are collected
  </action>
  <verify>
    <automated>pytest tests/test_llm_agent.py --collect-only -q | grep -c "test_"</automated>
  </verify>
  <done>Test stubs created and collected by pytest</done>
</task>

<task type="auto">
  <name>Task 2: Wire MistralModelManager to use real MistralClient.chat()</name>
  <files>kiro/voiquyr/src/agents/llm_agent.py</files>
  <action>
    Modify MistralModelManager class in llm_agent.py to use real Mistral API:

    1. Import the VoiceProcessingModels from src.api.models to access _mistral_client
    2. Add __init__ parameter to accept external client or use global instance
    3. Modify generate_response() to:
       a. Check if MistralClient available (from models._mistral_client)
       b. If available: construct chat request and call client.chat() with messages
       c. If NOT available: raise ConfigurationError("MISTRAL_API_KEY is required")

    4. Per D-04 in 03-CONTEXT.md: Replace mock fallback with ConfigurationError

    Key implementation (replace lines 253-258 existing mock check):
    ```python
    # Get Mistral client from VoiceProcessingModels
    from ..api.models import VoiceProcessingModels
    models = VoiceProcessingModels()
    
    if not models._mistral_client:
        raise ConfigurationError(
            "MISTRAL_API_KEY is required for LLM inference. "
            "Please set MISTRAL_API_KEY environment variable."
        )
    
    # Build chat request
    chat_request = {
        "model": "mistral-small-latest",
        "messages": messages,
        "temperature": 0.7,
        "top_p": 0.9,
        "max_tokens": 1024,
    }
    
    # Add tools if provided
    if tools:
        chat_request["tools"] = tools
    
    # Call Mistral API
    response = models._mistral_client.chat.complete(chat_request)
    return response.choices[0].message.content
    ```
  </action>
  <verify>
    <automated>python -c "from src.agents.llm_agent import MistralModelManager; print('MistralModelManager imports OK')"</automated>
  </verify>
  <done>MistralModelManager.generate_response() uses MistralClient.chat() from mistralai SDK</done>
</task>

<task type="auto">
  <name>Task 3: Remove _generate_mock_response function</name>
  <files>kiro/voiquyr/src/agents/llm_agent.py</files>
  <action>
    Remove the mock response generation function:

    1. Delete `_generate_mock_response()` method (lines 299-320)
    2. Delete `_load_mock_model()` method (lines 220-232)
    3. Remove any references to "mock" in generate_response()

    After removal, confirm:
    - grep -n "_generate_mock_response" src/agents/llm_agent.py returns no matches
    - grep -n "_load_mock_model" src/agents/llm_agent.py returns no matches
  </action>
  <verify>
    <automated>grep -c "_generate_mock_response\|_load_mock_model" kiro/voiquyr/src/agents/llm_agent.py</automated>
  </verify>
  <done>_generate_mock_response() and _load_mock_model() removed from codebase</done>
</task>

</tasks>

<verification>
- MistralClient imported and initialized from mistralai SDK
- generate_response() calls MistralClient.chat() (not local transformers)
- _generate_mock_response() does not exist in codebase
- Missing MISTRAL_API_KEY raises ConfigurationError
- test_llm_agent.py tests pass (or xfail appropriately)
</verification>

<success_criteria>
LLM-01: true - _load_mistral_small_31() uses real mistralai SDK (MistralClient.chat())
LLM-02: true - _generate_mock_response() removed - all generation through real API
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm/03-01-SUMMARY.md`
</output>