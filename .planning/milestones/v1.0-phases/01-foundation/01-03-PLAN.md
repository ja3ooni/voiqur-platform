---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: [01-02]
files_modified:
  - kiro/voiquyr/src/api/db.py
  - kiro/voiquyr/src/api/routers/auth.py
  - kiro/voiquyr/src/api/auth.py
autonomous: true
requirements:
  - FOUND-05
  - FOUND-06
  - FOUND-07
  - FOUND-08
must_haves:
  truths:
    - "Database schema creates users, sessions, knowledge_items, webhook_registrations, audit_logs tables"
    - "verify_token() decodes JWT and looks up user in PostgreSQL"
    - "POST /auth/register stores bcrypt-hashed password in users table"
    - "POST /auth/login returns signed JWT token"
  artifacts:
    - path: "kiro/voiquyr/src/api/db.py"
      provides: "Database schema creation"
      contains: "CREATE TABLE users"
    - path: "kiro/voiquyr/src/api/routers/auth.py"
      provides: "Auth endpoints (register, login, me)"
      contains: "router.post.*register"
    - path: "kiro/voiquyr/src/api/auth.py"
      provides: "JWT creation and verification"
      contains: "create_access_token"
  key_links:
    - from: "auth.py"
      to: "users table"
      via: "verify_token looks up user_id"
      pattern: "verify_token"
    - from: "register endpoint"
      to: "users"
      via: "INSERT INTO users"
      pattern: "INSERT.*users"
---

<objective>
Initialize database schema, implement auth endpoints (register/login/me), verify JWT works with real DB lookup.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Existing code patterns
@kiro/voiquyr/src/api/db.py
@kiro/voiquyr/src/api/routers/auth.py
@kiro/voiquyr/src/api/auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify database schema creation</name>
  <files>kiro/voiquyr/src/api/db.py</files>
  <read_first>kiro/voiquyr/src/api/db.py</read_first>
  <action>
Verify db.py contains create_schema() function that creates all required tables:

- users (id, email, username, hashed_password, is_active, is_verified, scopes, eu_resident, created_at, last_login)
- sessions (id, user_id, refresh_token, expires_at, created_at)
- knowledge_items (id, key, value, metadata, created_at, updated_at)
- webhook_registrations (id, url, events, secret, is_active, created_at, last_triggered)
- audit_logs (id, event_type, user_id, details, ip_address, created_at)

Existing code shows create_schema() function. Verify it exists.
  </action>
  <acceptance_criteria>
- db.py has create_schema function
- Function creates users table with required columns
- Function creates sessions table
- Function creates knowledge_items, webhook_registrations, audit_logs tables
  </acceptance_criteria>
  <verify>
<automated>grep -n "CREATE TABLE" kiro/voiquyr/src/api/db.py | head -10</automated>
  </verify>
  <done>Database schema initialization exists in db.py</done>
</task>

<task type="auto">
  <name>Task 2: Verify auth endpoints</name>
  <files>kiro/voiquyr/src/api/routers/auth.py</files>
  <read_first>kiro/voiquyr/src/api/routers/auth.py</read_first>
  <action>
Verify auth.py router has:

1. POST /register - Creates user with bcrypt-hashed password
2. POST /login - Returns JWT token
3. GET /me - Returns current authenticated user (real DB lookup)

Existing code shows all three endpoints. Verify:
- register uses hash_password() from auth_manager
- login calls auth_manager.verify_password()
- login returns auth_manager.create_access_token(user)
- me performs real DB lookup using token_data.user_id
  </action>
  <acceptance_criteria>
- POST /register stores bcrypt-hashed password
- POST /login returns JWT access_token
- GET /me performs real DB lookup
- Endpoints use asyncpg connection from pool
  </acceptance_criteria>
  <verify>
<automated>grep -n "router.post\|router.get" kiro/voiquyr/src/api/routers/auth.py | head -10</automated>
  </verify>
  <done>Auth endpoints implemented</done>
</task>

<task type="auto">
  <name>Task 3: Verify JWT verification with DB lookup</name>
  <files>kiro/voiquyr/src/api/auth.py</files>
  <read_first>kiro/voiquyr/src/api/auth.py</read_first>
  <action>
Verify AuthManager in auth.py has:

1. hash_password() - Uses bcrypt to hash passwords
2. verify_password() - Verifies bcrypt hash
3. create_access_token() - Creates JWT with user info
4. verify_token() - Decodes JWT and returns user_id (or looks up in DB if needed)

The /me endpoint in auth.py performs the DB lookup (see line 157-159), which satisfies FOUND-06.
  </action>
  <acceptance_criteria>
- hash_password uses bcrypt
- verify_password compares bcrypt hashes
- create_access_token signs JWT
- verify_token decodes JWT (user lookup in /me endpoint)
  </acceptance_criteria>
  <verify>
<automated>grep -n "hash_password\|verify_password\|create_access_token\|verify_token" kiro/voiquyr/src/api/auth.py | head -10</automated>
  </verify>
  <done>JWT verification with real DB lookup implemented</done>
</task>

</tasks>

<verification>
Run: Test auth flow with httpx TestClient - register user, login, access /me with token
</verification>

<success_criteria>
- create_schema() creates all required tables
- POST /auth/register stores bcrypt-hashed password
- POST /auth/login returns JWT
- GET /auth/me returns user from token
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>