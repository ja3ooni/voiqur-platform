---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: [01-00]
files_modified:
  - kiro/voiquyr/src/api/main.py
  - kiro/voiquyr/.env.example
  - kiro/voiquyr/.env
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - "load_dotenv() is called at module level in main.py"
    - ".env.example documents all required environment variables"
    - ".env contains placeholder values for local development"
  artifacts:
    - path: "kiro/voiquyr/src/api/main.py"
      provides: "load_dotenv() called at module level"
      contains: "load_dotenv()"
    - path: "kiro/voiquyr/.env.example"
      provides: "Environment variable documentation with descriptions"
      min_lines: 50
  key_links:
    - from: "main.py"
      to: ".env"
      via: "load_dotenv()"
      pattern: "load_dotenv"
---

<objective>
Wire load_dotenv() into main.py at module level, create .env.example with all required vars, and local .env.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Existing code patterns
@kiro/voiquyr/src/api/main.py
@kiro/voiquyr/src/api/config.py
@kiro/voiquyr/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify load_dotenv() in main.py</name>
  <files>kiro/voiquyr/src/api/main.py</files>
  <read_first>kiro/voiquyr/src/api/main.py</read_first>
  <action>
Verify load_dotenv() is called at module level BEFORE APIConfig() or any config instantiation.

Current code shows:
```python
from dotenv import load_dotenv
load_dotenv()  # Called before APIConfig()

app = create_app(APIConfig())
```

This is correct. If missing, add:
```python
from dotenv import load_dotenv
load_dotenv()  # At module level BEFORE config
```
  </action>
  <acceptance_criteria>
- load_dotenv() is called at module level (not inside a function)
- load_dotenv() appears BEFORE any APIConfig() instantiation
  </acceptance_criteria>
  <verify>
<automated>grep -n "load_dotenv" kiro/voiquyr/src/api/main.py | head -5</automated>
  </verify>
  <done>load_dotenv() called at module level in main.py</done>
</task>

<task type="auto">
  <name>Task 2: Ensure .env.example is complete</name>
  <files>kiro/voiquyr/.env.example</files>
  <read_first>kiro/voiquyr/.env.example</read_first>
  <action>
Verify .env.example documents all required environment variables with descriptions:

Required sections:
- Application (ENVIRONMENT, API_HOST, API_PORT, LOG_LEVEL)
- JWT (JWT_SECRET_KEY with generation hint)
- PostgreSQL (DATABASE_URL with DSN format)
- Redis (REDIS_URL)
- External APIs (MISTRAL_API_KEY, DEEPGRAM_API_KEY, ELEVENLABS_API_KEY, TWILIO_*, STRIPE_API_KEY)
- Webhooks (MAX_CONCURRENT_WEBHOOKS, WEBHOOK_TIMEOUT, etc.)

Add any missing variables with descriptive comments.
  </action>
  <acceptance_criteria>
- .env.example contains DATABASE_URL or similar
- .env.example contains REDIS_URL or similar  
- .env.example contains JWT_SECRET_KEY or similar
- All variables have descriptive comments
  </acceptance_criteria>
  <verify>
<automated>wc -l kiro/voiquyr/.env.example && grep -c "^[^#]" kiro/voiquyr/.env.example</automated>
  </verify>
  <done>.env.example documents all required variables</done>
</task>

<task type="auto">
  <name>Task 3: Create local .env from .env.example</name>
  <files>kiro/voiquyr/.env</files>
  <read_first>kiro/voiquyr/.env.example</read_first>
  <action>
Copy .env.example to .env (do NOT overwrite if .env exists with real values).

If .env does not exist, create it from .env.example with placeholder values:
- JWT_SECRET_KEY: generate with python -c "import secrets; print(secrets.token_hex(32))"
- DATABASE_URL: postgresql://user:password@localhost:5432/euvoice
- REDIS_URL: redis://localhost:6379
- API keys: "your-key-here" placeholders
  </action>
  <acceptance_criteria>
- kiro/voiquyr/.env exists
- Contains valid DATABASE_URL format
- Contains valid REDIS_URL format
- JWT_SECRET_KEY is not the placeholder text
  </acceptance_criteria>
  <verify>
<automated>ls -la kiro/voiquyr/.env && grep -v "^#" kiro/voiquyr/.env | grep -v "^$" | head -5</automated>
  </verify>
  <done>Local .env created with dev values</done>
</task>

</tasks>

<verification>
Run: cd kiro/voiquyr && python -c "from dotenv import load_dotenv; load_dotenv(); from src.api.config import APIConfig; print('Config works:', APIConfig().database_url)"
</verification>

<success_criteria>
- load_dotenv() called at module level in main.py
- .env.example documents all required vars with descriptions
- .env exists with local dev values
- Config loads without errors after load_dotenv()
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>