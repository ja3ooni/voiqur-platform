---
phase: 01-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - kiro/voiquyr/pytest.ini
  - kiro/voiquyr/tests/conftest.py
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - "pytest runs all tests in kiro/voiquyr/tests/"
    - "Test database and Redis connections use environment variables"
    - "Async tests work with pytest-asyncio"
  artifacts:
    - path: "kiro/voiquyr/pytest.ini"
      provides: "pytest configuration with asyncio_mode"
      min_lines: 10
    - path: "kiro/voiquyr/tests/conftest.py"
      provides: "Test fixtures for async tests"
      contains: "TEST_DATABASE_URL"
  key_links:
    - from: "pytest.ini"
      to: "tests/conftest.py"
      via: "asyncio_mode = auto"
      pattern: "asyncio_mode"
---

<objective>
Install testing dependencies and create pytest configuration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Project structure
@kiro/voiquyr/pytest.ini
@kiro/voiquyr/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest dependencies</name>
  <files>kiro/voiquyr/requirements.txt</files>
  <read_first>kiro/voiquyr/requirements.txt</read_first>
  <action>
Ensure pytest, pytest-asyncio, and httpx are in requirements.txt. Add if missing:

```
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
```

Also add pytest.ini to the kiro/voiquyr directory with:
- asyncio_mode = auto
- testpaths = tests
- python_files = test_*.py
- python_classes = Test*
- python_functions = test_*
- Add any existing pytest configuration.
  </action>
  <acceptance_criteria>
- kiro/voiquyr/pytest.ini exists and contains "asyncio_mode"
- kiro/voiquyr/requirements.txt contains pytest, pytest-asyncio, httpx
  </acceptance_criteria>
  <verify>
<automated>ls -la kiro/voiquyr/pytest.ini && grep -q "asyncio_mode" kiro/voiquyr/pytest.ini</automated>
  </verify>
  <done>pytest.ini exists with asyncio_mode configuration</done>
</task>

<task type="auto">
  <name>Task 2: Create test fixtures</name>
  <files>kiro/voiquyr/tests/conftest.py</files>
  <read_first>kiro/voiquyr/tests/conftest.py</read_first>
  <action>
Create or update tests/conftest.py with fixtures for:

1. TEST_DATABASE_URL - reads from environment or defaults
2. TEST_REDIS_URL - reads from environment or defaults
3. If services unavailable, tests skip gracefully using pytest.importorskip or pytest.mark.skip

Reference the existing db.py patterns:
- asyncpg pool creation
- redis.asyncio connection
  </action>
  <acceptance_criteria>
- conftest.py contains TEST_DATABASE_URL fixture
- conftest.py contains TEST_REDIS_URL fixture
- Tests can run without external services by skipping
  </acceptance_criteria>
  <verify>
<automated>ls -la kiro/voiquyr/tests/conftest.py && grep -q "TEST_DATABASE_URL" kiro/voiquyr/tests/conftest.py</automated>
  </verify>
  <done>Test fixtures created for database and Redis connections</done>
</task>

</tasks>

<verification>
Run: cd kiro/voiquyr && pytest --collect-only
</verification>

<success_criteria>
- pytest.ini exists with asyncio_mode = auto
- tests/conftest.py has TEST_DATABASE_URL and TEST_REDIS_URL fixtures
- pytest --collect-only runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md`
</output>