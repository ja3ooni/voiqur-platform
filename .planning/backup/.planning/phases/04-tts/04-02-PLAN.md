# Plan 04-02: XTTS-v2 Self-Hosted and Voice Cloning Engine

**Goal:** Implement the self-hosted XTTS-v2 path using the `TTS` library and create the `VoiceCloningEngine`.

**Requirements:** TTS-02, TTS-03
**Success Criteria:**
1. `TTS` library is integrated and XTTS-v2 model is callable (when installed)
2. `VoiceCloningEngine.clone_voice()` returns embeddings using `get_conditioning_latents()`
3. Tests in `test_tts_phase4.py` for TTS-02 and TTS-03 pass (un-xfail)

---

## Tasks

### 1. Install Dependencies
- Install `TTS>=0.22.0` and `torch>=2.0.0` (in a local dev environment)

### 2. Implement XTTS-v2 Self-Hosted Path
- Add lazy import for `TTS.tts.models.xtts.Xtts`
- Implement self-hosted synthesis in `XTTSv2ModelManager` as a fallback or explicit choice

### 3. Create `VoiceCloningEngine`
- Implement `VoiceCloningEngine` class in `tts_agent.py`
- Add `clone_voice()` method wrapping `model.get_conditioning_latents()`

### 4. Update Tests
- Remove `xfail` from `test_xtts_import` and `test_voice_cloning`
- Use `pytest.importorskip("TTS")` for XTTS tests
- Mock `get_conditioning_latents` for unit testing

### 5. Verification
- Run `pytest tests/test_tts_phase4.py::test_xtts_import -x`
- Run `pytest tests/test_tts_phase4.py::test_voice_cloning -x`
