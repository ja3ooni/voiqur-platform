# Plan 04-00: Phase 4 Wave 0 — TTS Test Scaffold and Dependency Guard

**Goal:** Establish the TTS contract with xfail stubs and fix top-level `torch` imports in `tts_agent.py` to allow test collection in lean environments.

**Requirements:** TTS-01, TTS-02, TTS-03, TTS-04, TTS-05
**Success Criteria:**
1. `pytest --collect-only tests/test_tts_phase4.py` succeeds without `ModuleNotFoundError`
2. `pytest tests/test_tts_phase4.py` reports 5 xfailed tests (no collection errors)
3. `src/agents/tts_agent.py` is importable even if `torch` is not installed (guarded by try/except)

---

## Tasks

### 1. Guard `torch` and `torchaudio` imports in `tts_agent.py`
- Wrap top-level `import torch` and `import torchaudio` in `try/except ImportError`
- Define `_TORCH_AVAILABLE` boolean
- Add a helper `_check_torch()` that raises `RuntimeError` if called when `_TORCH_AVAILABLE` is False
- Wrap existing `XTTSv2ModelManager` methods that use `torch` with this check

### 2. Create `tests/test_tts_phase4.py` with xfail stubs
- Implement `test_elevenlabs_synthesis` (TTS-01) — xfail: "ElevenLabs SDK not integrated"
- Implement `test_xtts_import` (TTS-02) — xfail: "XTTS-v2 library not integrated"
- Implement `test_voice_cloning` (TTS-03) — xfail: "VoiceCloningEngine not implemented"
- Implement `test_tts_streaming` (TTS-04) — xfail: "Real streaming not wired to ElevenLabs"
- Implement `test_e2e_pipeline` (TTS-05) — xfail: "Real TTS not wired into processing_pipeline.py"

### 3. Verification
- Run `pytest --collect-only` to ensure no import errors
- Run `pytest tests/test_tts_phase4.py` and confirm 5 XFAIL results
