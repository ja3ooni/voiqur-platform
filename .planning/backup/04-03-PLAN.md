# Plan 04-03: Pipeline Integration and E2E Tests

**Goal:** Wire the real TTS synthesis into the core processing pipeline and verify the full flow.

**Requirements:** TTS-05
**Success Criteria:**
1. `ProcessingPipeline._process_tts()` calls real `TTSAgent.synthesize_text()`
2. E2E test (audio in -> STT -> LLM -> TTS -> audio bytes out) passes
3. Final cleanup of any remaining TTS mocks

---

## Tasks

### 1. Wire TTS into `ProcessingPipeline`
- Update `ProcessingPipeline.__init__` to accept `tts_agent`
- Replace mock `_process_tts()` implementation with `self._tts_agent.synthesize_text()`
- Handle audio format conversion (numpy to bytes) in the pipeline

### 2. Update E2E Tests
- Remove `xfail` from `test_e2e_pipeline`
- Implement integration test with mocked STT and LLM but real TTS synthesis logic

### 3. Final Verification
- Run the full test suite: `pytest tests/ -x`
- Verify that `STT_IMPLEMENTATION_SUMMARY.md` is updated if necessary (though this is Phase 4)

### 4. Verification
- Run `pytest tests/test_tts_phase4.py::test_e2e_pipeline -x`
