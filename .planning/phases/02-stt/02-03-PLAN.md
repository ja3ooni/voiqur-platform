---
phase: 02-stt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - kiro/voiquyr/src/core/processing_pipeline.py
  - kiro/voiquyr/src/agents/stt_agent.py
autonomous: true
requirements:
  - STT-04
  - STT-05

must_haves:
  truths:
    - "processing_pipeline.py uses real VoxtralModelManager.transcribe()"
    - "xfail markers removed from test_stt_agent.py tests"
  artifacts:
    - path: kiro/voiquyr/src/core/processing_pipeline.py
      provides: Voice processing pipeline
      contains: process_audio, STT integration
    - path: kiro/voiquyr/tests/test_stt_agent.py
      provides: STT tests (updated)
      contains: passing tests

key_links:
  - from: processing_pipeline
    to: VoxtralModelManager.transcribe
    via: from ..agents.stt_agent import VoxtralModelManager
    pattern: VoxtralModelManager
---

<objective>
Wire real STT into processing pipeline and finalize tests

Purpose: STT-04 requires STT to be wired into the processing pipeline, STT-05 requires tests to pass. This plan integrates VoxtralModelManager into the main pipeline and ensures tests pass.

Output: Real STT in pipeline, passing tests
</objective>

<context>
@kiro/voiquyr/src/core/processing_pipeline.py
@kiro/voiquyr/src/agents/stt_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find and analyze processing pipeline</name>
  <files>kiro/voiquyr/src/core/processing_pipeline.py</files>
  <action>
    Read and analyze processing_pipeline.py:
    
    1. Find where STT is called
    2. Check if it uses VoxtralModelManager
    3. Find the audio processing flow
    
    Example pattern:
    ```python
    from ..agents.stt_agent import VoxtralModelManager
    
    async def process_audio(audio_chunk):
        stt = VoxtralModelManager()
        await stt.load_model()
        result = await stt.transcribe(audio_chunk)
        return result
    ```
  </action>
  <verify>
    <automated>python -c "from src.core.processing_pipeline import process_audio; print('Pipeline imports OK')"</automated>
  </verify>
  <done>Processing pipeline analyzed</done>
</task>

<task type="auto">
  <name>Task 2: Wire VoxtralModelManager into processing_pipeline</name>
  <files>kiro/voiquyr/src/core/processing_pipeline.py</files>
  <action>
    Ensure the processing pipeline uses VoxtralModelManager:
    
    1. Import VoxtralModelManager if not already imported
    2. Ensure transcribe() is called with real implementation
    3. Remove any mock/sine wave generation paths
    
    The pipeline should already use VoxtralModelManager, but verify:
    ```python
    from ..agents.stt_agent import VoxtralModelManager
    
    async def process_audio(audio_data):
        stt = VoxtralModelManager()
        await stt.load_model()
        result = await stt.transcribe(audio_data)
        return result
    ```
  </action>
  <verify>
    <automated>python -c "from src.core.processing_pipeline import process_audio; print('Pipeline uses real STT')"</automated>
  </verify>
  <done>Real STT wired into pipeline</done>
</task>

<task type="auto">
  <name>Task 3: Remove xfail markers and finalize tests</name>
  <files>kiro/voiquyr/tests/test_stt_agent.py</files>
  <action>
    Remove xfail markers from tests once implementation is verified:
    
    1. If tests were marked with @pytest.mark.xfail, remove the marker
    2. If tests were marked with @pytest.mark.skip, evaluate if still needed
    3. Run full test suite:
    
    ```bash
    cd kiro/voiquyr
    pytest tests/test_stt_agent.py -v
    ```
    
    All tests should pass or have clear skip reasons.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && pytest tests/test_stt_agent.py -v --tb=short</automated>
  </verify>
  <done>All STT tests passing</done>
</task>

</tasks>

<verification>
- processing_pipeline.py uses VoxtralModelManager.transcribe()
- test_stt_agent.py tests pass (or skip with valid reason)
- No xfail markers remain unless implementation incomplete
</verification>

<success_criteria>
STT-04: true - real STT wired into processing pipeline
STT-05: true - tests pass with real implementation
</success_criteria>

<output>
Create `.planning/phases/02-stt/02-03-SUMMARY.md` upon completion
</output>