---
phase: 02-stt
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - kiro/voiquyr/tests/test_stt_agent.py
  - kiro/voiquyr/requirements.txt
autonomous: true
requirements: [STT-05]

must_haves:
  truths:
    - "pytest tests/test_stt_agent.py collects 5+ test functions (not 0)"
    - "deepgram-sdk, mistralai, and langdetect are installable from requirements.txt"
    - "All collected tests are marked xfail (expected to fail until implementation)"
  artifacts:
    - path: "kiro/voiquyr/tests/test_stt_agent.py"
      provides: "Pytest async test scaffold for STT-01 through STT-05"
      contains: "async def test_"
    - path: "kiro/voiquyr/requirements.txt"
      provides: "New STT dependencies pinned"
      contains: "deepgram-sdk"
  key_links:
    - from: "kiro/voiquyr/tests/test_stt_agent.py"
      to: "kiro/voiquyr/src/agents/stt_agent.py"
      via: "import VoxtralModelManager, AudioChunk, LanguageDetector"
      pattern: "from src\\.agents\\.stt_agent import"

user_setup:
  - service: deepgram
    why: "Primary STT transcription provider"
    env_vars:
      - name: DEEPGRAM_API_KEY
        source: "Deepgram Console -> Settings -> API Keys -> Create Key"
  - service: mistral
    why: "Voxtral fallback STT transcription"
    env_vars:
      - name: MISTRAL_API_KEY
        source: "Mistral La Plateforme -> API Keys"
---

<objective>
Rewrite test_stt_agent.py from asyncio script to proper pytest, install STT dependencies.

Purpose: Wave 0 prerequisite — all subsequent plans need these tests and libraries to exist.
Output: Pytest-discoverable test file with xfail stubs; requirements.txt with deepgram-sdk, mistralai, langdetect.
</objective>

<execution_context>
@/Users/aljaunia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aljaunia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stt/02-RESEARCH.md
@.planning/phases/02-stt/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add STT dependencies to requirements.txt</name>
  <files>kiro/voiquyr/requirements.txt</files>
  <read_first>
    - kiro/voiquyr/requirements.txt
  </read_first>
  <action>
Append the following three lines to the `# STT Agent dependencies` section of `kiro/voiquyr/requirements.txt` (after the existing `websockets>=11.0.0` line):

```
deepgram-sdk>=3.1.0,<4.0
mistralai>=2.0.0
langdetect>=1.0.9
```

Then run `cd kiro/voiquyr && pip install deepgram-sdk>=3.1.0,<4.0 mistralai>=2.0.0 langdetect>=1.0.9` to install them into the active venv.

Do NOT change the deepgram-sdk pin to v4+ — the project uses v3 API surface (`listen.asyncprerecorded.v("1").transcribe_file()`). v4+ has incompatible API changes.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && python -c "import deepgram; import mistralai; import langdetect; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains the line `deepgram-sdk>=3.1.0,<4.0`
    - `requirements.txt` contains the line `mistralai>=2.0.0`
    - `requirements.txt` contains the line `langdetect>=1.0.9`
    - `python -c "import deepgram"` exits 0
    - `python -c "import mistralai"` exits 0
    - `python -c "import langdetect"` exits 0
  </acceptance_criteria>
  <done>Three STT libraries pinned in requirements.txt and importable in venv</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_stt_agent.py as pytest with xfail stubs</name>
  <files>kiro/voiquyr/tests/test_stt_agent.py</files>
  <read_first>
    - kiro/voiquyr/tests/test_stt_agent.py
    - kiro/voiquyr/src/agents/stt_agent.py
    - kiro/voiquyr/src/core/processing_pipeline.py
    - kiro/voiquyr/pytest.ini
  </read_first>
  <action>
Replace the entire contents of `kiro/voiquyr/tests/test_stt_agent.py` with a proper pytest file. The file must NOT contain `asyncio.run()` or `if __name__ == "__main__"`. The `pytest.ini` has `asyncio_mode = auto`, so no `@pytest.mark.asyncio` decorators are needed.

The file must contain these 5 test functions, ALL marked `@pytest.mark.xfail(reason="Not yet implemented — Wave 1/2")`:

1. `async def test_transcribe_returns_real_string()` — STT-01
   - Creates a `VoxtralModelManager` instance
   - Creates a 1-second 440Hz sine wave as `AudioChunk(data=audio, sample_rate=16000, timestamp=0.0, chunk_id=0)`
   - Calls `await mgr.transcribe(chunk)`
   - Asserts `result.text != f"Transcribed audio chunk {chunk.chunk_id}"` (not the mock string)
   - Asserts `isinstance(result.text, str) and len(result.text) > 0`

2. `async def test_voxtral_fallback()` — STT-02
   - Uses `monkeypatch.delenv("DEEPGRAM_API_KEY", raising=False)` to ensure Deepgram key absent
   - Creates a `VoxtralModelManager` instance
   - Creates same audio chunk as test 1
   - Calls `await mgr.transcribe(chunk)`
   - Asserts result is a non-empty string and not the mock string
   - Has `skipif` for `not os.getenv("MISTRAL_API_KEY")` with reason "MISTRAL_API_KEY not set"

3. `async def test_language_detection_returns_code()` — STT-03
   - Creates a `LanguageDetector` instance
   - Calls `await detector.detect_language("This is a test sentence in English")`
   - Asserts `result.language == "en"`
   - Calls `await detector.detect_language("Bonjour le monde")`
   - Asserts `result.language == "fr"`

4. `async def test_pipeline_stt_integration()` — STT-04
   - Imports `ProcessingPipeline` from `src.core.processing_pipeline`
   - Creates a pipeline instance
   - Calls `_process_stt()` with some audio bytes and a context
   - Asserts the returned transcription is NOT one of the hardcoded mock strings: `"Hello"`, `"Hello, how can I help you today?"`, `"Hello, how can I help you today? I'm here to assist with any questions you might have."`

5. `async def test_full_stt_suite_passes()` — STT-05
   - A meta-test that simply asserts True (placeholder to confirm the file collects)
   - Marked xfail with reason "Full suite depends on all other tests passing"

Required imports at top of file:
```python
import os
import pytest
import numpy as np
from src.agents.stt_agent import VoxtralModelManager, AudioChunk, LanguageDetector
```

Each test function must have a docstring referencing the STT requirement ID it covers.

Do NOT import `asyncio` or use `asyncio.run()`. Do NOT keep the old script structure.
  </action>
  <verify>
    <automated>cd kiro/voiquyr && pytest tests/test_stt_agent.py --co -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_stt_agent.py --co -q` reports 5 collected tests (not 0)
    - File does NOT contain `asyncio.run`
    - File does NOT contain `if __name__`
    - File contains `async def test_transcribe_returns_real_string`
    - File contains `async def test_voxtral_fallback`
    - File contains `async def test_language_detection_returns_code`
    - File contains `async def test_pipeline_stt_integration`
    - File contains `async def test_full_stt_suite_passes`
    - Each test function has `@pytest.mark.xfail`
    - `pytest tests/test_stt_agent.py -v` shows 5 xfail results (no errors, no unexpected passes)
  </acceptance_criteria>
  <done>pytest discovers 5 test functions; all 5 show as xfail; no collection errors</done>
</task>

</tasks>

<verification>
```bash
cd kiro/voiquyr && pytest tests/test_stt_agent.py --co -q
# Expected: 5 tests collected

cd kiro/voiquyr && pytest tests/test_stt_agent.py -v
# Expected: 5 xfailed (no errors, no unexpected passes)

cd kiro/voiquyr && python -c "import deepgram; import mistralai; import langdetect; print('All STT deps OK')"
# Expected: "All STT deps OK"
```
</verification>

<success_criteria>
- pytest collects exactly 5 tests from test_stt_agent.py
- All 5 tests are xfail (expected failures, not errors)
- deepgram-sdk, mistralai, langdetect are importable
- requirements.txt contains all three new dependency lines
</success_criteria>

<output>
After completion, create `.planning/phases/02-stt/02-00-SUMMARY.md`
</output>
